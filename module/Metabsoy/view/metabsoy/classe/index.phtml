<h1>Classes cadastradas</h1>
<div class="row">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Classe</th>
                <th>Opções</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->classes as $classe): ?>
                <tr>
                    <td><?php echo $this->escapeHtml($classe->getDescricao()); ?></td>
                    <td><a class="btn btn-primary btn-sm pull-center"
                           href="<?php echo $this->url('metabsoy/classe', ['action' => 'save', 'id' => $classe->getId()]) ?>"
                           title="Editar registro."><span class="glyphicon glyphicon-pencil"></span></a>
                        <a class="btn btn-primary btn-sm pull-center"
                           href="<?php echo $this->url('metabsoy/classe', ['action' => 'delete', 'id' => $classe->getId()]) ?>"
                           title="Excluir registro."><span class="glyphicon glyphicon-remove"></span></a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <span class="pull-right">
        <?php echo $this->paginationControl($this->classes, 'Elastic', 'metabsoy/partial/paginator', ['route' => 'metabsoy/classe']); ?>
    </span>
</div>

<div class="row">
    <a class="btn btn-primary pull-left" href="/metabsoy/classe/save">Novo registro</a>
</div>
