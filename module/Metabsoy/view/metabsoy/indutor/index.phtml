<h1>Indutores cadastrados</h1>
<div class="row">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Indutores</th>
                <th>Opções</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->indutores as $indutor): ?>
                <tr>
                    <td><?php echo $this->escapeHtml($indutor->getDescricao()); ?></td>
                    <td><a class="btn btn-primary btn-sm pull-center"
                           href="<?php echo $this->url('metabsoy/indutor', ['action' => 'save', 'id' => $indutor->getId()]) ?>"
                           title="Editar registro."><span class="glyphicon glyphicon-pencil"></span></a>
                        <a class="btn btn-primary btn-sm pull-center"
                           href="<?php echo $this->url('metabsoy/indutor', ['action' => 'delete', 'id' => $indutor->getId()]) ?>"
                           title="Excluir registro."><span class="glyphicon glyphicon-remove"></span></a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <span class="pull-right">
        <?php echo $this->paginationControl($this->indutores, 'Elastic', 'metabsoy/partial/paginator', ['route' => 'metabsoy/indutor']); ?>
    </span>
</div>

<div class="row">
    <a class="btn btn-primary pull-left" href="/metabsoy/indutor/save">Novo registro</a>
</div>
